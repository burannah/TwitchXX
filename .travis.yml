language: cpp
dist: trusty
sudo: required
compiler:
  - gcc
addons:
  apt:
    packages:
    # install toolchains
    - gcc
    - g++
    - cmake-data
    - cmake

before_install:
 - sudo apt-get install libboost-regex-dev libboost-system-dev libboost-thread-dev libboost-test-dev libboost-random-dev libcpprest-dev -y
env:
  global:
    - BOOST_INCLUDES=/usr/include
    - BOOST_LIBS=/usr/lib
before_script:
   - mkdir bin
   - cd bin
   - cmake ..

script: make

branches:
  only:
    - master
notifications:
  recipients:
    - burannah@gmail.com
  email:
    on_success: change